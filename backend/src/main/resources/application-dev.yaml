# Credenciais do Postgres local que o docker-compose cria. Não são segredo:
# Banco vazio, só escuta em localhost. Produção lê de variável de ambiente.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lineup
    username: lineup
    password: lineup

  jpa:
    properties:
      hibernate.format_sql: true
