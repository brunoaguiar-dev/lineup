spring:
  application:
    name: lineup-api

  profiles:
    default: dev

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

lineup:
  token:
    # Chave de assinatura em base64. Gere com: openssl rand -base64 48
    segredo: ${LINEUP_TOKEN_SEGREDO}
    validade-do-access-token: 15m
    validade-do-refresh-token: 7d
